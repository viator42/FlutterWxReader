
class Api {
  /*
  // 用户注册
  static Future register({String tel, String password}) async {
    return await http.post('/app/user/login/', {'tel': tel, 'password': password});
  }

  // 添加到书架
  static Future addToBookShelf({int userId, int bookId}) async {
    return await http.post('/app/book/shelf/add/', {'userId': userId, 'bookId': bookId});
  }

  // 从书架移除
  static Future removeFromBookShelf({int id}) async {
    return await http.post('/app/book/shelf/remove/', {'id': id});
  }

  // 书架列表
  static Future listBookShelf({int userId, int page}) async {
    return await http.get("/app/book/shelf/list/?userId=${userId}&page=${page}");
  }
  */

}

